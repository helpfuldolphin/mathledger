[project]
name = "mathledger-orchestrator"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "psycopg[binary]",
  "pydantic>=2",
  "redis",
  "orjson",
  "tenacity",
  "httpx",
  "jinja2",
]
[tool.uv]
