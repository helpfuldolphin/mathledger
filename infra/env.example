# Development Environment Configuration
# =====================================
#
# Copy this file to .env and fill in secure values before starting services.
#
# SECURITY NOTICE:
# - Never commit .env files with real credentials
# - Use strong, randomly generated passwords (minimum 12 characters)
# - Do not use default/weak passwords like 'mlpass', 'postgres', 'password'
#
# Generate secure credentials:
#   PostgreSQL password: openssl rand -base64 24
#   Redis password:      openssl rand -base64 24
#   API key:             openssl rand -hex 24

# PostgreSQL Configuration
POSTGRES_USER=ml_dev
POSTGRES_PASSWORD=<GENERATE_SECURE_PASSWORD>
POSTGRES_DB=mathledger

# Redis Configuration
REDIS_PASSWORD=<GENERATE_SECURE_PASSWORD>

# API Security
LEDGER_API_KEY=<GENERATE_48_CHAR_HEX_KEY>
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

