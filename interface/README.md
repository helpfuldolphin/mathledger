# Interface

External touchpoints converge here:

- Public and internal APIs
- UI applications and SDK wrappers
- Tooling used by integrators and auditors

Keep boundaries explicit: interface modules should delegate logic to ledger, normalization, or derivation layers rather than re-implementing them.

